{
  "name": "wasm-webp",
  "version": "0.0.1",
  "scripts": {
    "build": "gulp compile && cmake -DCMAKE_BUILD_TYPE=Release && make"
  },
  "dependencies": {
    "cross-env": "6.0.3",
    "gulp": "4.0.2",
    "gulp-replace": "1.0.0"
  }
}